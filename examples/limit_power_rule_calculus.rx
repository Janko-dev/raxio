lim(h, 0, (f(x + h) - f(x)) / h)
    
    f(a) => pow(a, 2) at 4
    
    pow(a + b, 2) => pow(a, 2) + 2 * a * b + pow(b, 2) at 4
    
    a + b + c - a => b + c at 3
    
    (a + b) / c => a/c + b/c at 1
    
    a * b * c / c => a * b at 2   
    
    pow(a, 2) / a => a at 2  
    
    lim(t, 0, a + t) => a at 0
    
end "example_results/limit_power_rule_calculus.txt"